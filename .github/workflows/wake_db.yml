name: DB Wake

on:
  schedule:
    - cron: "0 * * * *"

jobs:
  curl_job:
    runs-on: ubuntu-latest

    steps:
      - name: Executar comando curl
        run: |
          curl lucasgonc.vercel.app/send/wake -v
